import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'
import { faCode as icon } from '@fortawesome/free-solid-svg-icons'

export const EmbeddedIcon = () => {
    return <FontAwesomeIcon icon={icon} className="embeddedIcon" />
}